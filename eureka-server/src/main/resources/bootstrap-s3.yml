server:
  port: 8763
spring:
  application:
    name: demo-eureka-server
eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://september:demo@localhost:8761/eureka,http://september:demo@localhost:8762/eureka,http://september:demo@localhost:8763/eureka
