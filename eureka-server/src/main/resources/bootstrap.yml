server:
  port: 8762
spring:
  application:
    name: demo-eureka-server
eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8762

