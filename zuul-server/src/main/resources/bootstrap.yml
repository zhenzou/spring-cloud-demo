server:
  port: 9000
spring:
  application:
    name: demo-zuul-server
eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://september:demo@localhost:8761/eureka

zuul:
  ignored-services: '*'